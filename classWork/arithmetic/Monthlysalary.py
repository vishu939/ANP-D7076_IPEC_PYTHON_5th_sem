# Program to calculate total monthly salary

# Input basic salary and incentive
basic_salary = float(input("Enter the fixed salary: "))
incentive = float(input("Enter the incentive amount: "))

# Calculate total salary
total_salary = basic_salary + incentive

# Display result
print("Total Monthly Salary = ₹", total_salary)
